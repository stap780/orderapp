<%= form_for(@sskom) do |f| %>
  <% if @sskom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sskom.errors.count, "error") %> prohibited this sskom from being saved:</h2>

      <ul>
      <% @sskom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
