<p id="notice"><%= notice %></p>
<div class="col-md-12" style="margin-bottom:20px;">
	<div class="col-md-12">
		<div class="col-md-2">
			<h1>Наш склад </br><small>сводная таблица</small></h1>
		</div>
		<div class="col-md-3">
		</div>
		<div class="col-md-7">
		</div>
	<div class="col-md-12">
		<div class="col-md-8">
		<!-- link_to 'Оприходование', new_store_path -->
		</div>
	</div>
</div>

<div class="col-md-12">
<div class="media">
  <div class="media-body">
<table class="table table-hover">
  <thead>
    <tr>
      <th>Product</th>
      <th>Приход</th>
      <th>Расход</th>
      <th>Quantity</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stores.each do |store| %>
      <tr>
        <td><%= store.title %></td>
        <td><% b=store.stocks.where("purchase_list_id IS NOT ?", nil)%>
        <%b1= b.sum(:quantity) %><%=b1%>
        </td>
        <td><% c=store.stocks.where("invoice_list_id IS NOT ?", nil) %>
        <% c1 = c.sum(:quantity) %><%=c1%>
        </td>
        <td><%= a = "#{b1}".to_i - "#{c1}".to_i%><!-- store.quantity --></td>
        <td><%= store.price %></td>
        <td><%= link_to 'Show', store %></td>
        <td><%= link_to 'Edit', edit_store_path(store) %></td>
        <td><%= link_to 'Destroy', store, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @stores %>
  </div>
</div>
</div>
</div>
