<%= simple_form_for(@courier) do |f| %>
  <% if @courier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courier.errors.count, "error") %> prohibited this courier from being saved:</h2>

      <ul>
      <% @courier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :number %>
  </div>
  <div class="field">
    <%= f.input :date %>
  </div>
  <div class="field">
    <%= f.input :state, collection: ['Отгружен', 'Доставлен']%>
  </div>
<%= f.button :submit %>
<% end %>
