<%= form_for(@vimcom) do |f| %>
  <% if @vimcom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vimcom.errors.count, "error") %> prohibited this vimcom from being saved:</h2>

      <ul>
      <% @vimcom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :quantity_all %><br>
    <%= f.number_field :quantity_all %>
  </div>
  <div class="field">
    <%= f.label :quantity_free %><br>
    <%= f.number_field :quantity_free %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
